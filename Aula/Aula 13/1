#include <stdio.h>
#include <stdlib.h>

int main(){
    int d1, d2, soma=7;
    d1=0;
    d2=7;
    while (d1<7){
        while (d2>0){
        d1++;
        d2--;
        if(soma=d1+d2);
        printf("%d + %d = %d\n", d1, d2, soma);
        }
    }
    return 0;
}
